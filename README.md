# @wyx-ui/react

一个基础的 React UI 组件库起始包。

## 使用
```tsx
import { Button } from "@wyx-ui/react";

export default function Demo() {
  return <Button variant="primary">确定</Button>;
}
```

> 提示：你可以在应用侧引入基础样式，或后续在库内提供样式方案。
